server {
    listen 80;
    server_name _;

    # 首页门户
    location / {
        root /opt/tradingRecords/portal;
        index index.html;
    }

    # 交易记录系统 - 前端
    location /trading/ {
        alias /opt/tradingRecords/frontend/dist/;
        try_files $uri $uri/ /trading/index.html;
    }

    # 交易记录系统 - 后端 API
    location /api/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
