# AGENTS

本仓库协作约定：

- 本地调试统一使用 `./dev.sh`（up/status/attach/down/restart）。
- 生产部署统一使用 `deploy/update.sh`。
- 优化时要额外考虑易用性与美观
- 每次提交/推送前，必须同时更新 README.md 与 README.zh-CN.md；若确实无内容可改，也必须在回复中明确写“README 无需更新的理由”，未完成不得提交。
- 回复需要尽可能的简短精炼
- 提交前回复必须包含：README 改动文件路径 + 改动摘要；缺少该项视为未完成任务。
